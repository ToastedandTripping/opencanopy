---
status: in-progress
current: "Jul 16: CO₂ calculator redesign MERGED + DEPLOYED (main @ b0a1391, LIVE on opencanopy.ca, CI green; critic BLOCKED→8 must-fixes folded→delta CLEAR; Razor PASS after 1 re-review; Jen 10 concerns folded; 690 tests green) — the /map carbon calc was shipping a false public '0 tonnes CO₂'; replaced the render-dependent queryRenderedFeatures foundation with a WFS-fetch + turf-clip data path (real BC VRI, accuracy from the DATA not the VIEW), status-driven honest number (never a confident 0), rounded + one-sided band, watershed carbon descoped to an honest state. Deployed 2026-07-16 on top of the audit relay — AWAITING Lee's live browser QA only (keyless sandbox couldn't verify). ALSO Jul 16: the Jul-11 audit P0+P1 remediation was MERGED + DEPLOYED (main @ 3bb009e, then b0a1391 with the CO₂ merge on top, LIVE on opencanopy.ca, CI green; CSP report-only header live-verified). Prior — Jul 11: Fable-5 /production audit (grade D — 1 a11y FAIL / 31 WARN / 42 PASS over an otherwise-healthy codebase; report ~/marvin/state/production-audits/opencanopy-2026-07-10.md) → P0+P1 remediation COMPLETE on relay/audit-p0-p1-remediation (critic-gated, Razor PASS 16/16, Jen PASS, 622 tests/tsc/build/lint(0)/audit(0-high) green) — screen-reader a11y FAIL fix (scroll-story unhidden) + /map zoom/mousemove re-render cascade killed (readout extracted to <MapReadout>, DataLayer memoized) + report-only CSP + CI Lint/Audit gates made blocking. NOT deployed; AWAITING merge + deploy + live QA. Prior — Jul 5: Multiphase scroll-story plan approved (.claude/plans/sharded-finding-beacon.md, critic-gated 11P/5C/0F). Phase 0 SHIPPED + merged (27b8ff3): scrub-gate probe harness (npm run probe:scrub + scripts/probes/*.py) — Lee-terminal probe runs + type-swap deploy still pending (runbook ~/marvin/state/opencanopy-phase0-runbook.md, gate memo opencanopy-scrub-gate-memo.md). Phase 1 COMPLETE on relay/story-phase1-cleanup (Razor PASS 12/12, 603 tests/tsc/build/lint green) — dead-machinery removal (bearing-drift loop, terrain animation, hatch pipeline, 8 minzoom-9 vector layers + their PMTiles source, pinned-0 forest-age raster) + perf floor (per-frame setState equality guards + matchMedia cache, render-count guard test) + 404-quieting (bounds on binary source immediate; fail-open ocbin:// tile-manifest wrapper, benefit lands once Lee generates+deploys the manifest via rclone lsf). story-hillshade was mis-flagged as dead in the plan and RESTORED (renders live, env-gated on MapTiler key) — story stays visually identical. AWAITING: Lee review + merge (/save) + deploy + live QA (map visuals, binary reveal, 404 silence — keyless worktree can't render the map). Phases 2-5 HELD. NEXT: Phase 2 (dolly play-on-scroll video — notes in sharded-finding-beacon-phase2-notes.md). Prior — Jun 30: shipped Beat-2 copy rewrite ('Reckoning') + Literata/Public Sans type swap (Fraunces + Inter retired), build/tests green (deploy + /map font QA pending). Jun 28 shipped Phase 1b + Exploration Bridge (LIVE on opencanopy.ca): forest-base regenerated from real VRI (2048x1003, clean coast/lakes, replaces the dilated blob); binary end-reveal tiles z4-9 (old-growth #0d5c2a / everything else #ef4444) on R2 raster/v3/binary/ replacing the vri-logged PNG; STRtree spatial index added to build-raster-tiles.py (3-4hr build -> ~27min); ending dolly into a 'remains' chapter zooming to a Vancouver Island/Clayoquot old-growth pocket (deuteranopia-checked); text-first reveal (binary holds then fades after the panel lands); CTA reworked to 'Now find your own corner of it.' + 'Explore the Map' hand-off that opens /map at the exact pocket (forest-age on). Relay phase1b-exploration-bridge, Razor PASS. KNOWN ISSUE (deferred): the ending dolly is NOT smooth on live — drops tiles + laggy on the z5->z8 binary zoom."
next: "LIVE QA (Lee — keyless sandbox couldn't verify) the two deployed relays now on main @ b0a1391. CO₂ calc: draw a forested area → real non-zero tonnes, STABLE when you zoom/pan the same box; draw over ocean → 'No forest data' (NOT 0); select a watershed → 'not available for watersheds yet'; a huge draw → 'Area too large'; the Share text + exported PDF never say '0 tonnes' and their car/home equivalences match the rounded headline; sanity-check the 500 km² guard + 20s timeout feel right on real usage (retune if not) + whether the (responsive, chunked) turf-clip wait is acceptable or wants a Web Worker. audit-p0-p1: SR reaches all 6 chapters; /map pan/zoom smooth. CSP enforce-flip + P2 a11y cluster + P3 ops remain deferred follow-ups — watch the report-only CSP console cold-cache/incognito+mobile before flipping to enforcing CSP. TOP: Lee runs the Phase-0 probe/deploy runbook (feeds the Phase-3 gate) + reviews/merges/deploys Phase 1 (relay/story-phase1-cleanup) with live QA. Then Phase 2 (dolly → play-on-scroll pre-rendered video, replaces the janky live z5->z8 zoom; adaptation map in sharded-finding-beacon-phase2-notes.md). Then GATED Phase-3 scrub decision (WebGL year-raster ~8-10d vs 2048px frames ~2-3d — decided by the pre-committed memo thresholds after Phase 2 ships), Phase 4 creative pass + gold old-growth, Phase 5 z10+ rectangle tile rebuild. 404-quieting FOLLOW-UP: generate + deploy the binary tile-manifest (rclone lsf → scripts/generate-tile-manifest.py) to activate the ocbin:// ocean-tile suppression Phase 1 wired. Deferred: place names in copy, production audit WARNs (CSP, monitoring, rollback docs, window.__opencanopy_map prod-leak guard)."
testing: null
pinned: false
shipped:
  - date: 2026-07-16
    item: "CO₂ calculator redesign — render-independent WFS + turf-clip data path (relay co2-calculator-redesign, plan .claude/plans/jazzy-gathering-codd.md; critic-gated: original review BLOCKED [Completeness FAIL + X3/X4], all 8 must-fixes folded, delta re-review CLEAR; Razor PASS after 1 re-review [2 WARNINGs fixed: unbounded body-read timeout + missing race/guard tests], Jen CONCERNS [10, ALL folded]; 690 tests / tsc / build / lint(0) green). COMPLETE on relay/co2-calculator-redesign (forks main @ 3bb009e), NOT deployed. FIX: the /map carbon calculator was shipping a false, shareable '0 tonnes CO₂' LIVE — found in the 2026-07-16 map-audit fan-out, verified in code, DEAD TWICE OVER: (1) queried a layer id `layer-forest-age-fill` that never exists (forest-age is tile-only → `layer-forest-age-tiles-fill`, DataLayer.tsx:301); (2) even fixed, tiles rename props to class/age/species but the math reads PROJ_AGE_1/SPECIES_CD_1 (calculator.ts) → age null → 0. Plus render-dependent by design (viewport-only, drop-densest-thinned, none below z10). NEW (Option A): fetch real BC VRI polygons for the selection via the existing /api/wfs edge fn (probed LIVE — schema 100% present, 4.3s small / 22s large), clip to the drawn area with @turf/intersect (v7 `intersect(featureCollection([…]))`), sum by class/age → CO₂ — accuracy is now a property of the DATA, not the VIEW (same selection = same tonnes at any zoom). HONESTY (the point): status-driven headline (NEVER a confident 0 — ocean/empty → 'no data', WFS failure → 'unavailable'), rounded figures + one-sided band ('up to ~20% high' per the model's own overestimate bias), a real 'How this is calculated' <details> disclosure, Share/Export/PDF status-gated + rounded (no false-precision public number), equivalences derived from the rounded figure, aria-live states + prefers-reduced-motion guard. DESCOPED v1: watershed carbon (groups 1k–10k km² → latency/cap) → honest 'not available for watersheds yet — draw a custom area' (Jen caught the first-draft message reintroduced the misattribute-the-cause bug this relay exists to fix); Option C class-raster = named follow-up. Guard: pre-fetch bbox-area cap 500 km² (Ted measured 7 live latency samples), 20s hard timeout bounding the full fetch+read (Razor caught the first version only bounded time-to-headers); z12 area fidelity −0.37% mean err (15k feats). 2 new files (forest-carbon-client, clip) + page/panel/pdf/calculator edits; DataLayer untouched. Orchestrator Opus 4.8; Ted=Sonnet; Razor + plan-critic on Fable-5 (Fable ≥ Opus per Lee). Behavioral (2.8) SKIPPED — env (Chrome profile lock + keyless map); data path fully unit + LIVE-WFS tested, panel UI/draw = Lee post-deploy QA. AWAITING: merge + deploy + live QA (draw → real non-zero tonnes, stable across zoom/pan; ocean → 'no data' not 0; watershed → 'not available yet'; huge draw → 'too large'; Share/PDF never '0 tonnes'; retune the 500 km²/20s knobs if real usage diverges). Follow-ups: k/C_max yield-curve sourcing (data task), Option C watershed fast-path, a Web Worker for turf-clip wall-clock if QA finds the (responsive) wait too long, the PDF financial compliance/voluntary caveat (panel-only today)."
  - date: 2026-07-11
    item: "Audit P0+P1 remediation (relay audit-p0-p1-remediation, plan .claude/plans/jazzy-gathering-codd.md — critic gate CLEAR + 5 must-fixes folded; Razor PASS 16/16 coverage, 0 CRITICAL/0 WARNING/4 NOTE; Jen PASS a11y). COMPLETE on relay/audit-p0-p1-remediation, **DEPLOYED 2026-07-16** (merged → main @ 3bb009e, LIVE on opencanopy.ca; CI green on main; CSP report-only + a11y fix + /map cascade fix HTTP-verified live) — strict improvement, independent of the held Phase-2 dolly branch. From the 2026-07-11 Fable-5 /production audit (grade D, report ~/marvin/state/production-audits/opencanopy-2026-07-10.md: 1 a11y FAIL / 31 WARN / 42 PASS). (P0) a11y FAIL fix — removed `aria-hidden={!active}` from NarrativePanel so screen readers reach all 6 scroll-story chapters (was: only the active one; blind users tabbed past the whole narrative). Reading order verified coherent (each panel scoped to its own .story-step, DOM order = CHAPTERS order). (P1a) killed the /map zoom+mousemove re-render cascade — extracted the z/coord readout into a self-subscribing <MapReadout> child (useMap + .on/.off all three, same handler refs) so CanopyMap holds no per-frame state, + React.memo on DataLayer with a scoped `classFilters?.[layer.id]` by-value comparator; the 18-19 DataLayers no longer re-render on every zoom tick/mousemove. (P1b) report-only CSP in netlify.toml (enumerated origins R2/MapTiler/ssc-ops; `'unsafe-inline'` for static-export unnonced RSC scripts, `blob:` for the maplibre worker) — observes only; enforce flip deferred. (P1c) CI made able to catch regressions — eslint override for e2e/scripts tooling + the 3 src set-state-in-effect errors resolved (useDeviceCapability lazy-init [critic-verified hydration-safe via ssr:false]; useLayerState + useTimeline documented scoped-disable — the plan critic caught that lazy-init'ing useLayerState would ship a hydration bug unit tests can't catch), `npm audit fix` (2 moderates), `continue-on-error` removed from both CI Lint+Audit steps. 622 tests (614 + 8 net-new, both mutation-proven) / tsc / build / lint exit 0 / audit 0-high — all green (Razor re-ran every gate himself + live-reverted both new tests to prove them). Orchestrator Opus 4.8; Ted=Sonnet; Razor + plan-critic on Fable-5 (Fable >= Opus per Lee). Behavioral (Stage 2.8) SKIPPED — environment (Chrome profile lock + DevTools MCP disconnected + keyless map + ssr:false story), NOT a code result; a11y+memo unit-proven & Razor-mutation-verified. AWAITING: merge + deploy + live QA (SR reaches 6 chapters; /map pan/zoom smooth; readout clear of top-right nav; shared-link + returning-visitor load with no hydration errors; CSP console-watch cold-cache/incognito+mobile → flip to enforcing CSP). Deferred (tracked): CSP enforce flip, P2 a11y cluster (contrast/keyboard/focus/states), P3 ops (cache headers, hero srcset, prefetch gating, DEPLOY.md, scheduled audit:live, dep bumps, WFS limiter). Razor NOTE for the deferred preset styleOverrides/opacityScale batch (memory deferred_preset_opacity_overrides): the DataLayer memo keys on layer.id assuming reference-stable layer objects — that batch must NOT recreate layer objects with the same id or style changes get silently swallowed."
  - date: 2026-07-09
    item: "/map polish — 4 verified audit fixes (relay map-polish-fixes, Razor PASS 4/4, 0 CRITICAL/0 WARNING/3 NOTE; 614 tests/tsc/build/lint green). COMPLETE on relay/map-polish-fixes (commit f38cd1f), MERGE-READY (forks from origin/main, zero overlap with the held Phase-2 dolly branch, strict improvement — safe to merge independently; does NOT auto-deploy). From a Sonnet /map re-audit (report ~/marvin/state/opencanopy-map-audit-2026-07-09.md). (1) RASTER Z-ORDER: the forest-age raster overview <Layer>s rendered ABOVE basemap text labels (declarative JSX had no beforeId while vector layers correctly used firstSymbolId) → place names illegible under the raster at z4-9. Fix: getFirstSymbolId helper + a styledata-driven rasterBeforeId, beforeId wired onto ALL 5 raster layers (default + 4 per-class — per-class had the same bug on single-class filter). Razor-confirmed against react-map-gl source: beforeId change = map.moveLayer (reorder), never remove/add, so the deliberate anti-flash pre-mount is untouched; no re-render loop (setState bails on the stable primitive). (2) MOBILE 375px: the 4th preset chip 'Fire+Logging' was unreachable — a fixed bug-report button masked the (already-scrollable) row's right edge; fixed via pr-14 clearance + -webkit-overflow-scrolling:touch. (3) window.__opencanopy_map now gated behind NODE_ENV!=='production' (was an open prod-leak WARN since 06-25). (4) MapPopup friendly labels for the full closed set of raw lowercase keys (age/species/name/designation). Orchestrator Opus 4.8; Ted=Sonnet; Razor inherited Opus. NOTABLE: the audit's headline 'Critical raster coverage gap in N/NE BC' was orchestrator-VERIFIED as a FALSE POSITIVE (the correct BC province-boundary edge — every forested-interior tile serves 200; only out-of-province tiles 404) — NO tile rebuild; see memory map_audit_2026_07. OUT of this batch (deferred): the z10+ drop-densest artifact (Phase 5, re-confirmed live + worse than 'contained'), the invisible per-class raster 404-spam (gating risks a flash — low value). Keyless worktree: z-order legibility + mobile layout are Lee's live deploy QA. NOTE: session hit rate limits twice (Fable budget, then the session limit mid-Razor) — Razor re-run after the reset."
  - date: 2026-07-05
    item: "Scroll-story Phase 1 — dead-machinery removal + perf floor + 404-quieting (relay story-phase1-cleanup, Razor PASS 12/12 coverage, 0 CRITICAL; plan .claude/plans/sharded-finding-beacon.md Phase 1, critic-gated). COMPLETE on relay/story-phase1-cleanup, AWAITING Lee review+merge+deploy+live QA (NOT yet on opencanopy.ca). (1) DEAD MACHINERY removed — registered/allocated every load but never activated by the 6 chapters: bearing-drift rAF loop (no chapter sets bearingDrift), terrain-animation + fog effects (replaced by an unconditional one-time dark setSky in onLoad, byte-identical to the old load-bearing fog else-branch — sky stays dark), hatch pipeline (HatchPattern.ts deleted), 8 minzoom-9 PMTiles vector layers (story caps at z8 so unreachable) + the story's PMTiles source registration, story-forest-age-raster (pinned opacity 0 forever), applyTimelineFilter (died with its only consumer), prefetchTerrainTiles + forest-age raster warming. (2) PERF FLOOR — value-equality guards before the 4 per-frame setStates in useScrollytelling.updateCamera (camera 5-field compare, yearFilter/binaryOpacity scalar, overlays content-compare — was allocating fresh arrays every frame) so idle scroll frames stop re-rendering the container; matchMedia MediaQueryList actually cached (was re-queried every frame despite a 'cached' comment); real render-count guard test added (mutation-proven, not tautological). (3) 404-QUIETING — bounds on the story-binary-reveal source (cuts off-bbox R2 404s immediately) + a fail-open ocbin:// addProtocol tile-manifest wrapper (17 unit tests; missing/malformed/stale manifest passes every request through untouched — only ever suppresses a known-missing tile). FOLLOW-UP for the full ocean-tile win: Lee generates+deploys the manifest via rclone lsf → scripts/generate-tile-manifest.py (build-raster-tiles.py now emits one for future builds). CAUGHT + FIXED IN-BATCH: story-hillshade was mis-classified as dead in the plan but is env-gated on the MapTiler key and renders LIVE in prod — restored (static hillshade only; terrain animation stays deleted; paint props char-for-char identical) so the story is visually identical. Net -382 LOC. Invariants preserved (single-writer opacity contract, reduced-motion hold-then-snap e2e green, STORY_END_CAMERA SSOT). tsc/603 tests/static build/lint all green. Orchestrator Opus 4.8, Razor inherited it. Keyless worktree: map VISUALS (hillshade/sky/binary reveal) + the 404 silence are Lee's live deploy QA — unrenderable locally (no MapTiler key, R2 no-CORS to localhost)."
  - date: 2026-06-30
    item: "Hero Beat-2 copy rewrite (Jen 'Reckoning' direction) + type-system swap to Literata (display) + Public Sans (body/UI), retiring Fraunces + Inter (Fraunces was only shipped 06-27). COPY: collapsed the disjoint '8 million' / 'hectares.' / two-fragment stack into ONE red 'hectares' hero line + two complete sentences ('Logged since 1950. An area larger than Ireland.' / 'Most of it will never be old growth again.') — fixes Lee's 'reads disjointed' (defect was dangling fragments, not the facts). FONTS: Fraunces's WONK/quirk axis undercut data credibility (esp. the calc CO2 readout); Literata (opsz, no quirk, built for long-form reading) carries the elegy, Public Sans (US civic face, VERIFIED tabular figures) carries the number-heavy map UI. Verified empirically via fonttools: Literata's DIGIT subset ships tnum+pnum, so the animated calc CO2 counter is safe left on the display font (no jitter); Public Sans tnum solid across all subsets. tsc + next build (static export — proves Literata/Public_Sans resolve) + 606 tests + lint all green; hero live-verified on local static screenshots (fonts self-host → faithful render with no map/R2/MapTiler dependency). Specialists: Jen (copy directions), Beatrice (type sweep). DEFERRED (Lee's aesthetic calls, not bugs): move calc CO2 readout to Public Sans; unify the two big-number faces (hero stat = Literata display vs story year-counter = Public Sans body); CTA 14px eyebrow off the serif. Only local + /map font QA on deploy still pending (map UI now in Public Sans, unrenderable from a keyless worktree)."
  - date: 2026-06-28
    item: "Phase 1b + Exploration Bridge DEPLOYED to opencanopy.ca (relay phase1b-exploration-bridge, plan .claude/plans/curried-frolicking-reddy.md, critic-gated, Razor PASS). (1) forest-base.png regenerated from 6.2M real VRI polygons via build-year-overlays.py --dataset forest-base --width 2048 (2048x1003 paletted, all_touched=False -> crisp coastline + lakes-as-holes, 30.2% coverage; replaces the hand-drawn 1024x501 blob; live-eyeballed). (2) binary end-reveal: new 'binary' theme in build-raster-tiles.py (old-growth=palette old-growth #0d5c2a, mature/young/harvested=palette harvested #ef4444, palette-derived SSOT), z4-9 tiles (805) on R2 raster/v3/binary/ via rclone; replaces the vri-logged single-PNG overlay (vri-logged fully removed: source/layer/asset/build-year-overlays dataset). story-binary-reveal raster source mirrors story-forest-age-raster. (3) build-raster-tiles.py STRtree spatial index (features_in_bounds O(features*tiles) -> O(log n); 3-4hr -> ~27min; --verify-index equivalence 8/8). (4) ending dolly: dedicated 'remains' chapter (camera FLAT_BC -> cameraTo STORY_END_CAMERA = Vancouver Island/Clayoquot pocket z8, deuteranopia-checked green/red 4.47:1 under sim), eased cubic intra-chapter scrub (DOLLY_END 0.8), 800vh; ending tightened 600->300vh. (5) text-first: binary opacity moved to a per-frame single-writer (computeBinaryRevealOpacity, ending revealBinaryFadeIn [0.4,0.6]) so the panel text lands before the red; cutblocks 0.75 holds permit-red during the hold; 100ms transition (Razor ship-blocker fix: 600ms lagged the per-frame fade). (6) CTA 'fuller rework' (Jen spec + George copy): 'Now find your own corner of it.' / 'Your watershed, your valley, the old growth you've driven past. It's all in here.' + 'Explore the Map' continuity hash (/map#lat&lng&z&layers=forest-age opens at the exact pocket, verified live). 606 tests. KNOWN ISSUE deferred -> see below."
  - date: 2026-06-27
    item: "Public readiness pass: narrowed public surface (18→7 layers + satellite, presets 11→4), React error boundary for WebGL crashes, bug report button (mailto with map state), production audit #2 (63/100, up from 52, 2 a11y FAILs fixed via relay — error boundary role=alert + SearchBar ARIA combobox + stale focusedIndex fix). Creative dept audit (Jen/Beatrice/George/Saul) + design polish relay: Fraunces variable serif font, typography cohesion, copy fixes (8M hectares corrected from 5M, em dashes, curly apostrophes, ending heading shortened), emerald accent (#34d399), Beat 2 left-aligned asymmetry, Beat 3 bottom-anchor, animation differentiation, CTA cleanup ('See It for Yourself', 'Keep it running', GitHub demoted). Data provenance section (VRI, FTEN, Wildfire Service, TANTALIS, OGSR named with DataBC link). Map UI: coordinate readout (top-right), satellite in MapLegend, Beat 3 gradient fade. VRI-logged overlay pipeline (build-year-overlays.py --dataset vri-logged, 5.6M features) — functional but needs upgrade to proper raster tiles (Phase 1b)."
  - date: 2026-06-22
    item: "Hero texture: dimmed fog backdrop on Beat 2 ('8 million hectares') — the only flat, texture-less panel between two full-bleed photo beats. hero-fog.webp at opacity 0.16 / brightness 0.8, offset crop (70% 45%) so it doesn't mirror Beat 3's fog; a vertical gradient sealed solid-dark top/bottom (seam blend into Beats 1 & 3) and held at 0.66 through the middle so the red figure stays focal and the zinc subtext keeps WCAG-AA contrast; decorative (empty alt + aria-hidden). Reads as 'the fog rolling in' ahead of Beat 3's full fog. Harvested from the superseded session-a97dab WIP — its original used a `fixed inset-0` full-viewport backdrop that would bleed behind the sticky map AND re-open the seam the veil just closed, so this is scoped to the beat instead. Live-verified locally on a keyless dev server (the hero is a static import, renders independent of the ssr:false map; hero-fog is same-origin so no R2/CORS/MapTiler dependency). Merged marvin/session-529466 → main, deployed to opencanopy.ca. RESOLVES the session-a97dab branch: its committed work is already in main (defer-layer-visibility-to-idle, prefetch+footer = merge-base PR #5, all 3 story images) and its 06-16 WIP was a parallel early run at the 06-18/06-21 redesign that shipped better — only the fog backdrop was unique, now harvested; a97dab safe to retire (on origin + a worktree)."
  - date: 2026-06-21
    item: "Reduced-motion scrollama crash FIXED + DEPLOYED to opencanopy.ca (origin/main, merges cd942bf + veil; live-verified). Root cause (empirically confirmed via a deterministic Playwright harness): `.story-step { min-height: auto !important; }` in the reduced-motion CSS block computes to 0px and collapses every scroll step (their only height is inline `${scrollHeight}vh`; NarrativePanel is absolute) → scrollama .setup() builds a non-finite IntersectionObserver threshold → throws → the whole story collapses (body ~2950px vs ~12670px). FIX = remove that one declaration. THE CSS FIX WAS CORRECT ALL ALONG — the earlier 'it didn't work, it's a JS timing bug' detour was a STALE-CSS TESTING ARTIFACT: HMR/`next dev` doesn't reliably recompile CSS for in-place mutation testing (Razor rebuilt the harness on a static `next build`+serve `out/`, verifying the served CSS at the wire), and Playwright `test.use({reducedMotion})` doesn't apply the @media query (use `page.emulateMedia({reducedMotion:'reduce'})`). Added `e2e/reduced-motion-scrollama.spec.ts` regression guard (mutation-PROVEN: re-adding the override fails tests 1-3 with the exact crash + 2950px collapse), Razor PASS-WITH-WARNINGS (4 advisory notes). LIVE-verified on opencanopy.ca: reduced-motion → no crash, body 13990px, counter holds→snaps to 2025 (1c works for real with same-origin R2 overlays); normal mode sweeps. PLUS hero→map seam veil (commit a96fcfd): a 100px `#0a0a0c`→transparent gradient on the sticky map's top edge (pointer-events-none) closes the luminance step over the grey landmass that Jen flagged PASS-WITH-CONCERN on the first deploy — re-verified live. Lightweight path: orchestrator-diagnosed (Playwright) → Ted impl → Razor → deploy → Jen + behavioral live gates; no full relay."
  - date: 2026-06-21
    item: "Story-render Phase 1 'client wins' (Batch A) — relay story-render-batch-a, branch relay/story-render-batch-a (forked off session branch 3419f6a; NOT yet merged to main). VERIFIED + shippable (regresses nothing): (1a) rAF-gate the scroll→year scrub — scrollama onStepProgress/onStepEnter now coalesce through a single rAF (shared pendingRef, newest-wins with index in the payload; setActiveChapterIndex + progressRef stay synchronous; pending frame cancelled+nulled on both teardown and unmount). One map update per frame instead of per scroll tick. Behaviorally verified: year counter advances 1950→2009 smoothly, zero per-tick console errors. (item 2) hero Photo-1 bottom gradient 0.85→1.0 (full dark, matches Photo 2) — Jen PASS, no Beat-1 over-darken. (1c) reduced-motion hold-then-snap LOGIC (unit+mutation-verified): inside the existing prog>=scrubStart gate, prefersReducedMotion() snaps scrubYear=scrub.end and skips the sweep, preserving each chapter's hold (the fire 'hold on red, then wildfire' beat survives). 543 tests (532+11), tsc/build clean. Razor PASS after a test-integrity re-review (3 mutation-PROVEN WARNINGs — coalescing-count, pure-stale-index, teardown-specific — + 1 dead placeholder, all fixed/deleted). Plan .claude/plans/inherited-painting-wigderson.md (critic-gated, 13 PASS/3 CONCERN/0 FAIL, all folded in). DEFERRED follow-up (Lee's call): 1c is DORMANT under prefers-reduced-motion because of a PRE-EXISTING scrollama setup-timing crash — .setup() computes a non-finite IntersectionObserver threshold (useScrollytelling.ts:196) and the whole story section collapses. Surfaced by the behavioral eval. NOT caused by Batch A — live on opencanopy.ca since repo init (d5c1749, 2026-03-21). RESOLVED 2026-06-21 — see the entry above: the .story-step min-height:auto override WAS the cause and removing it WAS the fix; the mid-session 'reverted, it's a JS timing bug' note here was a stale-CSS testing artifact, now corrected."
  - date: 2026-06-20
    item: "Deep research sweep + consolidated story-render plan (critic-gated; ~/marvin/research/scroll-driven-temporal-map-animation-20260620/). Q: best architecture for a scroll-driven temporal map animation that's smooth AND crisp at province scale on MapLibre. 37 sources, 3 finalist depth dives, 3 adversarial critics. VERDICT: current ~150 pre-rendered 1024px PNG frames = right family, poor impl. Coastline crispness ≠ overlay res (it's a line → needs a clean vector/high-res base, not upsampling). TARGET = GFW technique: year-encoded raster (1 byte/pixel, year−1949) + MapLibre custom WebGL decode shader (MapLibre has NO raster-color → custom layer is the only path; ~8-10d honest). Ruled out: video/WebCodecs (seek latency), sparse-keyframe crossfade (double-exposes cumulative data — correctness bug). Parked: deck.gl DataFilterExtension (filters after buffer alloc; province-scale crash risk unless COUNT(*)<~100k). Consolidated 5-phase plan written."
  - date: 2026-06-20
    item: "Story base fix DEPLOYED: scrollytelling no longer uses the forest-age overview raster as its base. After v3 made that overview disturbance-dominant (red), the story falsely implied BC was logged before 1950. visibility.ts now holds story-forest-age-raster at opacity 0; the green forest silhouette (story-forest-base) is the substrate + per-year cutblock overlays paint the loss on top — the intended 'green start, logging accumulates' narrative. Decouples the story from FOREST_AGE_RASTER_URL so future raster changes can't re-break it. 532 tests; live-verified (baseline green+sparse pre-1950 red; timeline accumulates). main d21a79e. KNOWN follow-ups surfaced: (a) static base is a hand-rolled over-dilated blob that bleeds into water + floods lakes/alpine green (no generator script exists) — Phase 1 of the plan; (b) z10+ rectangular artifact on the interactive /map forest-age vector tier."
  - date: 2026-06-20
    item: "v3 raster paint-order fix DEPLOYED: province-scale forest-age now reads RED. Root cause — build-raster-tiles.py PAINT_ORDER painted old-growth/mature LAST so with all_touched=True green overwrote harvested in every mixed low-zoom pixel → the z5 view falsely read as intact green (97.6% green) and logging only appeared on zoom-in. Reversed PAINT_ORDER (intact first, disturbance last); rebuilt the combined forest-age raster z4-10 (2895 tiles) → R2 raster/v3/; bumped FOREST_AGE_RASTER_URL. Per-class isolation tiles (paint-order-independent) stay at v2. z5 now 65% disturbance / 35% intact (same opaque-pixel count; only class resolution changed). Favours showing disturbance REACH over exact area in mixed pixels — the honest framing for a forest-loss map (Lee approved the trade-off). main 369b4d3, live-verified at z5. CONFIRMED data artifact (separate, queued): z10+ rectangular holes on the interactive forest-age vector tier = tippecanoe --drop-densest-as-needed + -M 5000000 dropping dense z12 features with no raster safety net past z9 (source VRI at the pin is 15/15 organic stands — the rectangles are ours, not the data)."
  - date: 2026-06-19
    item: "a11y grade-C FAILs closed + Wave 3 styling pass (branch marvin/session-529466, DEPLOYED 06-20). a11y: the nested-<button> FAIL was already fixed by the 06-18 LayerPanel redesign (regression test green); text-zinc-600 → text-zinc-400 at 11 informational sites (clears WCAG AA — zinc-500 would still fail at small text per the audit's own WARN). Wave 3 (Jen-spec'd values, Lee-approved): conservancies line→fill (cool slate #cbd5e1 faint fill + dashed white outline); cutblocks fill→boundary (near-zero 0.04 fill + bold red #dc2626 outline 0.9 — de-collides from forest-age harvested #ef4444, which was a fill-vs-fill clash); species-at-risk circles bumped (radius 5/7/10, opacity 0.85, pale-amber #fde68a halo). Supporting: optional `dasharray` on style.outline (PMTiles render path only — WFS path ignores style.outline, caveat documented); opacity-audit refined to credit outline-dominant fill layers by their boundary opacity (Math.max with style.outline.opacity); LayerPanel + MapLegend swatches render bordered/dashed for boundary layers (legend↔map fidelity, caught in review as a self-introduced regression). Parks (3.5) was already shipped 06-18. 3 parallel review agents: 2 correctness clean, cleanup findings folded in. 532 tests / build / 0-new-lint green. DEFERRED (Lee): 3.8 preset opacity overrides AND cutblocks boundary-aware timeline — both in the high-risk DataLayer opacity path, one batch. Visual verify pending on deploy (local can't render faithfully: no worktree MapTiler key, WFS species needs the /api/wfs proxy, R2 serves tiles cross-origin without CORS to localhost)."
  - date: 2026-06-18
    item: "Low-zoom legibility + old-growth swatch + LayerPanel a11y DEPLOYED & live-verified on opencanopy.ca (plan .claude/plans/misty-cuddling-yeti.md, critic-gated; main 8366792). DIAGNOSIS that reshaped scope: the 12 secondary layers were never 'locked at z10' — PMTiles build z4-12 and render from z0; they painted at 0.18-0.40 alpha (invisible, not absent). FIX = registry opacity retune, NOT raster expansion. All 12 legible by ~z8.5; parks 0.1 white-wash → emerald fill + crisp outline. Old growth (tap-deferrals): dashed gold line → solid dark-green fill + solid gold border, now tile-backed (reuses the forest-age source filtered class=old-growth at z10+ and the existing old-growth raster z4-9 — zero new pipeline) via a new generic style.outline primitive; WFS path inert so popups show PMTiles attrs (class/age); resolves WFS-fragility (P2) for it. species-at-risk circles bumped perceptible (3-8px + stroke). LayerPanel: all categories expanded by default, demoted to quiet labels, rows collapsed to a single role=switch — CLEARS BOTH grade-C a11y FAILs (nested <button> + text-zinc-600 contrast); first LayerPanel test added. Oracles updated (opacity-audit parks); stale tap-deferrals WFS-line refs cleaned (dual-source-status, satellite-zorder, schema-audit, e2e live-health, dead --color-tap-deferrals CSS var). 532 tests, build clean (MapTiler key verified in out/), lint 0 errors. Live-verified z8/z11/z14. Deferred: the zoom re-render cascade through 18 DataLayers (the z5-7 slowness root)."
  - date: 2026-06-18
    item: "Ko-fi support link added to the landing footer (ko-fi.com/toasted40013) as an emerald give-back CTA; external link + rel=noopener, AA contrast on the dark footer, aria-label. main 8366792, deployed + live-confirmed via curl."
  - date: 2026-06-18
    item: "Landing-story refinement DEPLOYED (plan .claude/plans/swirling-chasing-goose.md, critic-gated; main 1b61b01). Foundational: overlay opacity decoupled from yearFilter via a per-chapter `overlays` declaration (StoryMap is sole writer of each overlay's raster-opacity). New beat flow: hero(photos + red '8 million') → baseline(pre-1950 red fades in scroll-coupled, no counter) → timeline(inverse-cumulative scrub blended 0.4 toward linear so 1950-1980 reads, 800→600vh, flat camera) → fire(amber wildfire 1917-2025 over persisted red, holds on red+text ~22% before the orange begins, steady LINEAR counter, 'wildfires' label) → ending(scars persist + closing stat '35,000 ha of BC's largest old growth remain', Price/Holt/Daust 2020). Deleted zoom-in/old-growth-hatch/explore 3D beats (machinery left inert). New: build-scrub-tables.py (cumulative-area pacing curves), build-year-overlays.py parametrized --dataset {cutblocks|fire} + --mode {age-graded|additive}, 109 fire PNGs (1917-2025, 768px), scrub.ts + boundary tests. 522 tests, source-registration oracle 13→14. Favicon replaced with old-growth-rings mark (icon.svg + 3-frame .ico + apple-icon). Hero figure corrected: '5 million' → true 8.24M ha gross / 8.19M polygon (FTEN dated 1950-2025), 'larger than Ireland', softened 'never recovered' → 'most of it will never be old growth again'."
  - date: 2026-06-18
    item: "/production audit = grade C (52/100, report at marvin state/production-audits/opencanopy-2026-06-16.md). 5 specialists, 0 security/data FAILs; 2 a11y FAILs (LayerPanel nested <button> breaks AT; text-zinc-600 fails WCAG AA). Top WARN themes: no React error boundary, MapTiler key needs domain-lock, no CSP, zoom-event re-render cascade through 18 DataLayers, no uptime/error monitoring, no rollback doc, 112KB core-js polyfill to WebGL-only browsers. DataLayer.tsx flagged by 3 specialists (highest-risk surface). Remediation plan in the report, relay-ingestible."
  - date: 2026-06-12
    item: "Layer-fidelity Wave 2 DEPLOYED: per-class isolation rasters regenerated for all 5 themes z4-z10 in registry palette, uploaded to R2 raster/v2/ (12,798 tiles, local↔R2 parity verified), client flipped. Fixes the opaque-black class-toggle blanking; old-growth renders dark-green (gold theme deleted). Branch relay/layer-fidelity-w2 merged."
  - date: 2026-06-12
    item: "Layer-fidelity Wave 2 code (branch relay/layer-fidelity-w2, 6 commits, 511 tests): per-class isolation raster themes in registry palette (old-growth #0d5c2a dark green — gold theme deleted), forest-age-palette.json as single color authority for python script + TS registry (+ CalculatorPanel/pdf-generator stragglers), client flipped to raster/v2/ R2 dirs (old dirs kept one release), RASTER_THEME_COLORS override removed, color-audit rewritten with cross-language --dump-themes guard (Razor: PASS-WITH-WARNINGS → all closed → PASS). Plus simplify-for-raster.py prep: full-fidelity NDJSON (9.8GB = ~63GB RSS) exceeds the 32GB build machine; half-pixel-at-z9 simplify feeds the raster build only."
  - date: 2026-06-12
    item: "Layer-fidelity Wave 1 DEPLOYED + live-verified: proxy bbox-in-CQL fix (fish-streams/tap-deferrals/cutblocks all return features live; D3 e2e gates green), satellite z-order verified on production (index 26, all 38 overlays above, labels on top), MapTiler backdrop-dark confirmed zero layer-* collisions. Deploy hotfix en route: Netlify edge bundler treats every top-level file in netlify/edge-functions/ as an entry point — helper modules must live in a subdirectory (wfs-bbox-url.ts → lib/). Deploys are CLI/API-driven (npx netlify-cli deploy --prod --no-build after local build; CI is test-only, no auto-deploy on push)."
  - date: 2026-06-11
    item: "Layer-fidelity Wave 1 (branch relay/layer-fidelity-w1, 7 commits, 487 tests): proxy bbox-in-CQL fix (restores fish-streams, tap-deferrals + cutblocks supplemental — GeoServer rejects bbox+cql_filter together), imperative satellite z-order (anchor below first overlay/symbol, toggle-order independent), WfsLayers hooks-order fix + tile-backed fetch skip (StrictMode dev crash gone). Razor FAIL→fixed→PASS-WITH-WARNINGS; behavioral eval PASS on Wave 1 checks. Pre-existing finding logged: LayerPanel button-in-button nesting (Wave 3 ride-along)."
  - date: 2026-06-09
    item: "Production-readiness & shareability push (branch relay/production-readiness, 5 commits, +~1.5k LOC, 462 tests): per-layer error/empty/zoom status states (the centerpiece — replaces silent blank failures), OG/Twitter share card + metadataBase (blank previews fixed), privacy page + footer, WFS-proxy per-IP rate limiting, GitHub Actions CI, next 16.2.7. Live verification caught + fixed false 'BC data unavailable' errors on tile-backed layers during WFS hiccups. TileProgress dead code removed; pickDefinedPaint centralizes invariant #4."
  - date: 2026-04-13
    item: v9 PMTiles deployed (refactored bulk-FGDB pipeline, single-pass tippecanoe, Razor C1+W1-W4+N1 fixes)
  - date: 2026-04-10
    item: Pipeline refactor — bulk download + single-pass tippecanoe, replacing legacy monolith
  - date: 2026-04-08
    item: Forest-age vector tiles at z10, MapLibre v5 compatibility fix
  - date: 2026-04-01
    item: Tier 1 overview + z12 PMTiles rebuild (v8)
  - date: 2026-03-31
    item: Full data fidelity pipeline — 7-audit suite, water subtraction via GDAL/GEOS (63 min vs 72h projected)
---

# OpenCanopy — Roadmap

BC forest map: faithful government data rendered as PMTiles, served as a
public conservation reference. Architecture decision in March: base tileset
is government-truthful only; synthesis layers (fire-age reconciliation,
disturbance models) live as separate layers and are not part of the
open-source base.

## Known issues (deferred, prioritized)

- **Ending dolly is not smooth (TOP next-session item).** The scroll-coupled
  zoom into the Vancouver Island pocket (z5→z8 on the binary raster tiles, in the
  `remains` chapter) **drops tiles and lags** on the live site — confirmed by Lee
  2026-06-28. The 2026-06-28 smoothing pass (800vh spread, cubic ease, DOLLY_END
  0.8, path-prefetch of `cameraTo` viewports, 100ms transition) **reduced but did
  not fix** it. Likely remaining causes to investigate: (a) per-frame `map.jumpTo`
  on a 3-zoom-level change still forces MapLibre to churn tile levels faster than
  the binary raster (R2) can serve; (b) prefetch may not cover every intermediate
  zoom/tile along the dolly path; (c) 256px binary tiles over-zoomed near z8 thrash.
  Fix ideas for next time: prefetch ALL z5–9 tiles in the pocket corridor before
  the dolly starts (not just endpoints); consider easing the zoom depth back
  (z8→~z7) to cut tile load; or reconsider the mechanism (e.g. a pre-rendered
  zoom or a coarser-but-complete tile set). Code: `useScrollytelling.updateCamera`
  (cameraTo scrub), `prefetch.ts` `prefetchBinaryTiles`, `chapters.ts` `remains`.
- **Empty-tile 404 console noise.** Both `raster/v3/forest-age` (pre-existing) and
  the new `raster/v3/binary` raster sources request empty/ocean tiles the build
  skips → 404s (MapLibre renders nothing = correct, but it's console noise). A
  tile bounds/manifest so MapLibre never requests skipped tiles would clean it.

## Reference

- Architecture: `ARCHITECTURE.md` (in-repo, authored 2026-06-10)
- Audit pipeline: `scripts/audit-*.ts` (7-audit suite — source-to-tile tracing,
  geometry precision, spatial validation, adversarial testing, cross-source
  consistency, deep property validation, unified runner)
- Repo: github.com/ToastedandTripping/opencanopy
- Outstanding audit items (non-blocking):
  - Okanagan Lake V3 test point needs adjustment
  - tenure-cutblocks DISTURBANCE_START_DATE property issue
  - Cross-source R1 reclassify as informational (BC VRI data limitation)
- From the 2026-06-09 production push (for the follow-up /code-refresh):
  - **WfsLayers** has `if (layer.tileSource) return null` BEFORE its hooks (rules-of-hooks
    smell). Causes a dev-only StrictMode cleanup crash (`map.getMap()` undefined in the
    teardown). Pre-existing on main; production unaffected (effects run once). Fix the
    hooks order + guard the cleanup.
  - `conservation-priority` (258K fill from z0) left ungated — no degradation measured at
    z5; revisit with a real perf trace before gating `tileSource.minZoom`.
  - Empty/zoom/WFS-only-layer rendering verified by unit tests + wiring, NOT live (the
    `/api/wfs` edge fn doesn't run under `next dev`). Smoke-check on the live deploy.
  - Vestigial WFS fetch for tile-backed layers (data isn't rendered, fetch still fires) —
    candidate to skip entirely as an optimization.
