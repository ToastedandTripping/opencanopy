# Code Refresh: OpenCanopy -- 2026-09-01

## Baseline

836 tests green, tsc clean, `next build` clean, eslint 0 errors / 4 warnings, npm audit 2 high (dev-only)
-> 856 tests green, tsc clean, build clean, eslint 0 / 0, npm audit unchanged (blocked, see below) -- branch `refresh/2026-09-01`, 9 commits on origin/main `11b79f2`.

Test count moved 836 -> 828 (8 `normalizeAngle` cases deleted with the dead function) -> 856 (+28 real guards).

**Baseline correction first.** The session worktree had forked from `dolly/phase2-video`, the docked
dolly branch, not main; local `main` was itself 9 commits behind origin. The refresh was rebased onto
origin/main before anything was read. Worth knowing: `~/Projects/opencanopy` was left on a non-main
branch (memory `worktree_spawn_staleness`).

Four Fable readers by subsystem (map rendering; registry/proxy/data; story/routes/panels/carbon;
scripts/e2e/config/docs), 74 findings. The orchestrator re-verified every D-bucket item against source
before triage. Doctrine held: nothing was rewritten; the two files everyone points at (DataLayer.tsx
1385 lines, map/page.tsx 1111) were read end to end and judged long, not costly -- their cost sits in
three specific seams, all routed below.

## What changed (bucket A, 9 commits, each verified with tsc + eslint + the touched suites)

1. `df32442` **lint** -- stale eslint-disable in DataLayer (dead since the global-state switch), the unused proxy `_context` param + `NetlifyContext` interface (the 08-05 "Netlify signature requirement" claim was wrong), two unused test imports. 4 warnings -> 0.
2. `15e27ed` **dead code** (-424 lines) -- map barrel trimmed to CanopyMap; `LAYER_REGISTRY` barrel re-export; `CLASS_LEGEND_LABELS` (zero importers AND its label had drifted from the 2026-08-26 relabel); `ForestCarbonErrorKind`; `FLAT_BC_CAMERA`/`BINARY_TILE_PROTOCOL` un-exported; `normalizeAngle` + its 8 tests; 6 orphaned PresetChips glyphs; `public/images/map-preview.webp` (unreferenced since the first commit); in scripts/lib the never-wired 240-line `printLayerDashboard` block, `computePerLayerScore`, `traceFeatures`, `clearTileCache`, `checkPresence`, `tileBounds`, `tileCenter`. Every deletion by reference scan, not knip alone (knip was wrong about `fingerprintScore`, which is called in-file -- caught by tsc, kept as module-private).
3. `cb22257` **useMapState** -- ~20 lines of `style=` URL plumbing that could never emit (single `dark` style since the 08-05 basemap-switcher removal).
4. `a2aefe5` **dedup** -- three inline public-id filters -> `validateLayerIds`; `clear` = `disableMode` (byte-identical bodies); SearchBar zoom ladder (2 of 3 branches assigned the value already set); the 08-05 `prefersReducedMotion` dedup finished (stubs deleted, mid-file imports hoisted, HeroSection's missed inline `matchMedia` on the shared helper).
5. `a45fcf8` **debug health report** -- the 08-05 T0 fix left the WFS branch reporting "WFS source missing" for every enabled public layer (all tile-backed, none mount WfsLayers); now excluded. Loading indicator set/clear made symmetric through `shouldSurfaceWfsLoading` (identical on every reachable path).
6. `056cfb3` **scripts / e2e / config** -- `SOURCE_TO_MAPLIBRE` pointed the screenshot e2e at `layer-conservation-priority-tiles-fill`, a MapLibre layer that stopped existing at the 2026-08-26 rename (and at the non-public tenure-cutblocks entry); `verify.ts` imports the canonical source-layer list instead of a third copy; `audit-all` header said 7 (runs 8); the production monitor gets its own Playwright config so `npm run test:e2e` can no longer fire at opencanopy.ca; the tile-upload script reads the R2 origin from `r2-config.ts` (last inline R2 URL outside the SSOT) and names scripts that exist.
7. `3045aec` **audit tests, revert-proven** -- schema-audit Check 12 tested a hand-copy of `PRIORITY_KEYS` that had already drifted from MapPopup (3 keys missing, 1 extra) while green, plus a literal `expect(true)`; the list now lives in `src/lib/map/popup-keys.ts`, imported by both, and the WFS-side set is parsed from the proxy's `PROPERTY_WHITELIST`. zoom-handoff-audit derived each tier's min from the previous tier's max, so `findGaps()` could not return anything and ignored `tileSource.minZoom`; rewritten against registry values (zoomRange coverage, gate vs zoomRange[0], overview vs native tile max). timelineField "each is truthy" -> pinned set. registry-audit "documents filter intent" (re-asserted Check 2) deleted. NEW: legend swatch == paint colour per classSlug (ARCHITECTURE invariant 3 claimed it, nothing checked it; forest-age's legend hexes were literals, now `FOREST_AGE_PALETTE`); the 2000 ha cutblock cap is `CUTBLOCK_AREA_CAP_HA` on the registry side and pinned to the proxy CQL; property-schema keys ⊆ extractor output. Each guard: broken, watched go red, restored.
8. `cd47e40` **story/pipeline tests, revert-proven** -- text-level guard for the 2026-06-21 reduced-motion crash (`.story-step` inside a reduced-motion block; the only prior guard was a Playwright spec CI never runs); visibility.ts idle-deferral actually asserted (deleting the `map.once` line used to leave every test green); keyless tests pin the MapTiler key empty (with a key exported: 10 failed before, 0 after); the CHAPTERS===5 pin moved out of an a11y test it did not guard into the story audit.
9. `5657c95` **docs** -- ARCHITECTURE (setFilter -> global-state; the status-clears-on-unmount sentence that led the 08-05 fix astray; audit count; four missing dirs; story/map shared scrub tables), README ("coming soon"; "17 layers" naming five gated-off layers against CHARTER non-goal 2; stale presets; "accuracy improves with zoom"), CONTRIBUTING (recipe produced an invisible layer -- no `PUBLIC_LAYER_IDS` step), METHODOLOGY (Limitation 2 described the dead queryRenderedFeatures path), ROADMAP Parking Lot (the docked-dolly `git revert 72cbddb` recipe already conflicts -- verified by merge-tree -- replaced with a tag checkout), and the in-code comments that still narrated per-tick setFilter and pre-D10 dual-source rendering.

## Blocked (needs Lee's terminal)

- **CI on main is red and has been since 2026-08-22** (runs 32549982078, 33034689162, 33221729619), on the `npm audit --audit-level=high` step only -- tests, build and lint pass. Cause: `nanoid < 3.3.18` via `@tailwindcss/postcss -> postcss`, plus two `browserslist` advisories via `eslint-config-next`. Both dev-only; both semver-patch. The permission classifier blocked `npm audit fix` from this session. Fix:
  ```
  npm audit fix && npm ci && npm test && npm run build && npm audit --audit-level=high
  git add package-lock.json && git commit -m "deps: npm audit fix (nanoid, browserslist; dev-only patches)"
  ```
- **Unlisted transitive dependencies** (R4-14): `@mapbox/vector-tile`, `pbf`, `@types/geojson` are imported by scripts/lib and src but reach the tree only through maplibre-gl. A MapLibre 6 bump would break `audit:tiles/fidelity/precision/adversarial` at first run, not at upgrade time. `npm i -D @mapbox/vector-tile pbf @types/geojson` at the currently-hoisted versions. Same classifier block.

## Bugs found (bucket D -- route to a fix relay; not folded into the refresh)

Ordered by public impact. The first three are in the six public layers' only render path.

| # | Finding | Evidence | Fix shape | Needs Lee |
|---|---|---|---|---|
| R1-02 | **Timeline year silently dropped whenever a tile is in flight.** `page.tsx:326` guards `setGlobalStateProperty` on `map.isStyleLoaded()`, which in MapLibre 5.21 is `Style.loaded()` = false while ANY tile manager has a tile pending; `setGlobalStateProperty` only throws on `!style._loaded` (style JSON unparsed, impossible after `load`). Dropped ticks are never retried. The honest-timeline promise (CHARTER non-goal 3) fails in the slow-load case it was built for. Verified against `maplibre-gl-dev.js` 60071-60082 / 60148-60151. | `src/app/map/page.tsx:323-328` | Gate on a `styleReady` flag set once on `load` (or `map.getStyle()` non-null) and re-run the effect when it flips; add a mock `calls.setGlobalState` (R4-16) and a test. Live QA item. | No |
| R1-03 | **The 08-05 stale-status fix (1c37d21) never runs for any public layer.** The `!visible -> clearLayerStatus` branch sits below `if (... || hasTileSource) return` at `DataLayer.tsx:1143`; all six public data layers are tile-backed. A PMTiles error toast persists after the layer is toggled off, exactly the defect the fix was for. | `DataLayer.tsx:1143-1152` | Move the clear above the early return (or into the visibility effect); rendered PmtilesLayers test (R1-13). | No |
| R1-05 | **PmtilesLayers adds layers from a stale closure.** `addLayersToMap` reads `visible`/classFilters captured at mount; the reactive effects bail with `if (!getLayer(...)) return` while the source is still loading. Toggle a tile-backed layer ON during the (seconds-long on mobile) header wait: legend checked, map empty, until toggled twice. SatelliteLayers fixed the same race with `visibleRef` on 2026-06-11 (42ab047). | `DataLayer.tsx:237-245, :290, :448-475` | Mirror the `visibleRef` pattern; rendered test; live QA. | No |
| R2-02 | **wfs-client bookkeeping race.** The `finally` deletes `debounceTimers`/`controllers` by layer id without checking they belong to that call, so after an abort a newer timer/controller is dropped: duplicate upstream fetches on rapid pans and a superseded viewport's response can win. `rejectPendingForLayer` never rejects, so debounced-away promises hang. Original code, zero unit tests (08-05 COV-1 still open). Only WFS-only layers (non-public today). | `src/lib/data/wfs-client.ts:34-49, :96-100, :137-143` | Identity-check before delete (`if (controllers.get(id) === controller)`), reject orphaned pendings; ship with the missing tests. | No |
| R3-03 | **Unknown-age stands earn full stumpage but zero carbon.** `carbonPerHa` clamps missing `PROJ_AGE_1` to age 0 (C=0) while `calculateFinancialValue` credits 200 m³/ha x $24 -- the protect-vs-log chart is tilted toward logging by every unknown-age polygon. A number the data cannot support in both directions (CHARTER done-3). | `calculator.ts:116-118, :249; markets.ts:62` | Rule choice: exclude unknown from both sides, or show it as its own "unknown" bar with no dollar value. One line either way. | **Yes -- which rule** |
| R3-08 | **Hero says "8 million hectares logged since 1950"; the repo's own scrub table totals 7,061,938 ha.** Commit 8a84648 (2026-06-18) counted raw FTEN (8.24M gross / 8.19M footprint); `build-scrub-tables.py` sums the same field over the same date range from the *preprocessed* checkpoint (water-subtracted, deduped) -- the same file the red overlay is drawn from. Either preprocessing legitimately removed ~1.2M ha (then 7.06M is "what the map shows" and the copy overstates by 14%, and "larger than Ireland (7.0M ha)" becomes marginal), or the checkpoint drops real area. | `HeroSection.tsx:125`, `chapters.ts:142`, `src/data/scrub/cutblocks-scrub.json` total | One-time reconciliation of gross-vs-footprint-vs-preprocessed, then a guard test that the hero figure is derived from (or within a stated tolerance of) the scrub total. | **Yes -- which number is honest** |
| R3-12 | **Privacy page claims the analytics tracker's source is in this repo.** It loads from `ssc-ops.netlify.app/tracker.js`; no tracker code exists in the repo. Falsifiable in one click by the journalists the CHARTER names. | `privacy/page.tsx:29, :79-80` vs `layout.tsx:80-85` | Vendor `tracker.js` into `public/` and point the Script at it, or name the real origin and repo. | **Yes -- remedy** |
| R3-04 | **PDF footer points at "methodology at opencanopy.ca"; no such route exists.** | `pdf-generator.ts:287`; `ls src/app` | Print the GitHub METHODOLOGY.md URL the landing Footer already uses. | No |
| R4-02 | **`audit:live` has two tests that fail by construction on production** -- they assert `tenure-cutblocks` and `fish-streams`, both gated off by `PUBLIC_LAYER_IDS` (the 2026-08-26 fix 71ddef2 corrected only old-growth-250). Monitoring signal is zero until fixed; each burns 3 retries x 60 s. | `e2e/monitoring/live-health.spec.ts:170-201, :225-266` | Switch to `cutblocks`; hit `/api/wfs?layer=fish-streams` directly instead of via the UI. Needs a real run against prod to confirm. | No |
| R4-01 | CI red on main (see Blocked). | `ci.yml:33-36` | `npm audit fix` | Terminal |

## Addressed after review (2026-09-02, Lee's rulings: underestimate rather than state what cannot be sourced)

Three of the D-bucket items were charter violations needing a ruling, not a refactor. Lee ruled on all three; applied on this branch with revert-proven guards:

- **R3-08 hero figure** (`d28d811`). Recomputed from the FTEN checkpoint: dated 1950-2025 and under the 2,000 ha cap = 5.888M ha; the 230 polygons at 2,000-92,000 ha (2.45M ha, no client, repeated areas) are tenure boundaries that the /map layer already excludes and the story never did -- the scrub table summed 7.06M and the year overlays painted the tenure polygons red/maroon. The June "8 million" is not reproducible from the data. Hero now "5.8 million hectares ... larger than Nova Scotia" (floor to one decimal; Ireland no longer holds), chapter "nearly 6 million". Cap applied in `build-scrub-tables.py` and `build-year-overlays.py`; scrub JSON regenerated (declares `areaCap`); 76 overlay PNGs regenerated. Guard `hero-figure.test.ts`: hero over the data, more than 0.2M under it, "over 8 million" in the chapter, or a scrub table without the cap all fail.
- **R3-03 unknown-age stands** (`e73ecae`). Rule: if we cannot age it we do not price it. `TIMBER_VOLUME.unknown` 200 -> 0, ecosystem services exclude `unknownHa`, panel and PDF label the bar "excluded from all values", METHODOLOGY documents the class. Guard proven both ways.
- **R3-12 privacy page** (`4ece3eb`). `public/tracker.js` vendored from ssc-ops (8 KB, sessionStorage only), `layout.tsx` loads `/tracker.js`, CSP script-src drops ssc-ops (connect-src keeps it), the page names the receiving host and the fields it did not list (page URL/title/referrer, screen + viewport size). Guard `privacy-claims.test.ts` pins same-origin script, no cookies/localStorage in it, every sent field disclosed, host named.

Also **R3-04** remains open (PDF methodology pointer) and R4-02 (live monitor) -- unchanged, routed.

## Route to relay (bucket B -- refactors that need the test net)

- **R1-13 PmtilesLayers has zero rendered tests** -- the sub-renderer every public layer goes through. Two of three T1 lifecycle fixes (04f19bb, 1c37d21) shipped with no guard; dual-source-status, the two z-order suites and loading-context all stay green if the guarded code is deleted. The d10 test file already has a working `DataLayer` render harness with `useMap` mocked, and the mock has `_emit`. Write these alongside R1-02/03/05 so each fix is revert-proven.
- **R4-16** the MapLibre mock stubs `setGlobalStateProperty` without recording it; the page.tsx year-sync effect is unpinnable until `calls.setGlobalState` exists. Pair with R1-02.
- **R1-08** hash->layers resolution now lives in two halves (useLayerState.parseLayersFromHash vs useMapState.handlePopState + page.tsx handleLayerRestore); the 08-05 "reverse coupling" reason to decline is gone because useMapState already imports four helpers from useLayerState. Export one `resolveLayersFromHash` and call it from both.
- **R2-03** `DataFetchError` unified the class, not the semantics: forest-carbon-client has a 20 s timeout, composed abort, 429->rate-limit with Retry-After; watershed-client has none of those; wfs-client lets a network failure escape as a raw TypeError. The `"abort"` kind is never constructed. Bring the two thinner clients up to the carbon client's contract; re-verify DataLayer and useWatershedSelection catch paths.
- **R2-15** proxy `fetchWithRetry` retries deterministic failures (400 bad CQL, 404, ExceptionReport) and can spend 93 s on a request no client waits >20 s for. Retry only network/5xx/upstream-timeout; cap wall-clock; pure predicate module + test.
- **R3-07** map/page.tsx: seven of its eight responsibilities are legitimately a shell; the CO2 calc state machine (~110 lines: fetch -> schema-drift -> clip -> stats, seq-token supersession, error->copy mapping, Retry-After deadline) is business logic that is untestable in place. Extract `useCarbonCalculation`.
- **R3-10** "calculator must match METHODOLOGY.md exactly" is enforced by nothing; the 08-05 negative-age clamp has no regression test; class boundaries (80/250) and stumpage are asserted only as `> 0`. Constants DO match today (verified row by row). Pin them.
- **R4-03** the screenshot "layer isolation" suite never enables a layer (12 identical default-view screenshots) and half its rows are non-public. Needs a keyed run on Lee's machine to seed baselines.
- **R4-04** geometry-precision and spatial audits exclude `conservation-priority` on a stale "not present in current PMTiles build" premise; the reader read the live v10 header: present at z4-12, ~5M features, backing the public tap-priority layer. Include it; a human reads the first run's WARNs.
- **R2-01** age-class SSOT (T2) is one canonical definition plus copies in the proxy (unavoidable, Deno), `scripts/lib/extractors.ts` (avoidable: it already imports from `src/`), `audit-spatial.ts` and the registry CQL. Only two are regex-guarded. Delegate the extractor to `classifyForestAge`.

## What was deliberately left (the "no" list)

- **DataLayer.tsx split** -- long, not costly. Three sub-renderers are consistent where it matters (style-load guard, before-firstSymbol insertion, pickDefinedPaint, cleanup shape); the deliberate asymmetry (PMTiles never removes its layers: shared source) is load-bearing. The two real seams are R1-03 and R1-05. A split would re-step on every `fix(DataLayer)` landmine in the log.
- **map/page.tsx** beyond the calc extraction -- the other seven jobs are shell composition.
- **R1-15** WfsLayers class-filter effect overwrites the registry base filter (PmtilesLayers composes them) -- no WFS-only layer has a base filter or classSlugs, none is public. Dormant; documented in the types CAVEAT rather than touched.
- **R1-16** the visibility-change reload effect double-fires `loadData` (its identity already changes with `visible`) -- non-public path, the abort is swallowed, fold into R1-03.
- **R2-07** source-layer name `conservation-priority` behind registry id `tap-priority` -- renaming end-to-end is a tippecanoe rebuild + R2 flip for symmetry.
- **R2-16** four hardcoded layer ids outside registry/presets (scented-track `fire-history`, story visibility `forest-age`, the exclusivity pair, the calculator's VRI layer) -- each has a written rationale and lives outside components; ARCHITECTURE's "components never special-case an id" holds for `src/components`.
- **R3-05** `lib/timeline/scented-track.ts` imports the `ScrubTable` type from `lib/story/scrub` -- type-only, the data table already lives in the neutral `src/data/scrub`; documented in ARCHITECTURE as the one deliberate shared data dependency instead of moving one interface.
- **R3-11** camera interpolation / pitch generality that every flat chapter now bypasses -- schema-level generality with a control-chapter test; only `normalizeAngle` and the `FLAT_BC_CAMERA` export were dead.
- **R3-13** the `ocbin://` tile-manifest protocol is inert until a manifest is deployed and adds one 404 per landing load -- an artifact deploy, not a code change (`rclone lsf` -> `generate-tile-manifest.py`).
- **R3-20** bug-report mailto built in three places -- one string constant vs a page.tsx edit with no test net.
- **R3-21** LayerPanel CATEGORIES lists accountability/water/species, unreachable under the public gate -- load-bearing for the planned re-widening; the null-return is what keeps it correct.
- **R4-11** `deno.lock` is a stale package.json mirror -- zero runtime effect, and whether Netlify's edge bundler ever reads it was not verified; regenerate on the next `netlify dev` rather than delete on a guess.
- **R4-18** scripts/lib math (tile-math, quality-score, geometry-precision, feature-matcher) has zero unit tests and vitest cannot see `scripts/` -- worthwhile, but a test-net build for the fidelity suite, not a refresh action. Parking Lot candidate.
- **MapLibre 6.x, TypeScript 7, ESLint 10** majors -- unchanged from the 08-05 "no" list; not a cleanup.

## Recommendations beyond this skill's scope

- **Two CHARTER "done" clauses have live counter-examples**: done-3 ("never show a number the data does not support") is contradicted by R3-08 (hero figure), R3-03 (unknown-age asymmetry) and R3-12 (privacy claim). None is a refactor; each needs Lee's ruling on which statement is true. Suggest a `DECISIONS.md` entry for the cutblock-hectares definition (gross vs footprint vs preprocessed) once decided -- it will be re-litigated by Phase B's leaderboard otherwise.
- **The 2026-08-05 refresh's remediation relays (T0-T3) partially missed**: three of their fixes (stale-status clear, health report WFS branch, dual-source gating extraction) landed on paths the public layers never take. The pattern: DataLayer's tile-backed early return at :1143 makes everything below it dead for the public surface, and ARCHITECTURE said statuses clear on unmount. The doc line is fixed here; the code fixes are R1-03/R1-06 (the latter applied). Worth a rendered PmtilesLayers test before the next DataLayer relay (Batch 2 paint re-spine touches it).
- **The audit suite's own math is unguarded** (R4-18) -- the CHARTER phase-1 gate is "a green fidelity audit suite" and the suite's verdicts come from ~700 lines of untested pure functions.

## What to watch

- The three routed DataLayer bugs (R1-02/03/05) share one file and one test harness; do them as one relay with the PmtilesLayers rendered tests, before Batch 2 (paint re-spine) edits the same effects.
- `CUTBLOCK_AREA_CAP_HA` is now the single number; Phase B's leaderboard must apply it or the public hectares figure diverges from the map (scented-track.ts already warns about this).
- `deno.lock` will keep drifting until something regenerates it.
- `SOURCE_TO_MAPLIBRE` now maps to PUBLIC registry ids; if a non-public layer is re-enabled, its row needs the matching id.
- `playwright.live.config.ts` duplicates the base config's timeouts; change both or extract.
- The keyless-test env pin: `vi.hoisted(stubEnv)` in source-registration.test.ts runs before static imports by design -- if that file ever gains a keyed static-import test, it will need the dynamic-import pattern the file already uses at the bottom.

## Drift vs ARCHITECTURE.md

**Code fixed to match the doc (2):** invariant 3 (legend colours) now enforced and forest-age's legend sourced from the palette; the last inline R2 URL (tile-upload script) now reads the SSOT.

**Doc fixed to match the code (7):** useTimeline mechanism (global-state, not setFilter); status-clear lifecycle (always-mounted, clear on toggle-off); audit count and which audits run separately; §Layout missing four directories; useDeviceCapability is a story hook; story/map shared scrub tables named; story overlay builders in §Pipeline.

**Verified clean (readers, spot-checked by orchestrator):** invariant 2 (pickDefinedPaint) in all three sub-renderers; deterministic z-order; R2 URL SSOT in src/; dual-source status rule at its one reachable call site; story<->registry import separation (one type-only crossing, documented); 18-entry registry with 7 public exactly as stated; layer-id aliases on every input path; proxy origin lock, parameter clamping, CQL construction, rate-limit keying; carbon constants vs METHODOLOGY.md row by row; the 2026-06-21 reduced-motion fix still absent from globals.css.

**Doc still ahead of code (1):** the "Dual-source rule" is honoured by the `hasTileSource` early return, not by the extracted `resolveWfsStatus`, which is constant-folded at its only call site (R1-04, symmetry applied here; the extraction itself is harmless but the dual-source test's tile-backed half exercises a branch DataLayer cannot reach -- R1-13).

## Reader summaries

Raw findings (JSONL, one per line, all `file:line`-evidenced): `.refresh/2026-09-01-findings/findings-R{1..4}.jsonl`; the Stage-6 equivalence review is `.refresh/2026-09-01-findings/review-stage6.md`. Counts: R1 17 (3 D / 2 B / 8 A / 3 C + 1 cross-slice), R2 18 (1 D / 2 B / 11 A / 4 C), R3 21 (4 D / 3 B / 11 A / 3 C), R4 18 (2 D / 3 B / 12 A / 1 C).
