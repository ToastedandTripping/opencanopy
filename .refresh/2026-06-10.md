# Code Refresh: OpenCanopy — 2026-06-10

Branch: `refresh/2026-06-10` · Scope: whole repo · Oracle: ROADMAP.md +
(from this pass forward) the newly authored `ARCHITECTURE.md`.
Mid-refresh, Lee delivered a live-site layer audit; its findings were
diagnosed empirically in-session and folded with the refresh's
behavior-changing leftovers into `.claude/plans/layer-fidelity-2026-06-10.md`
(critic-gated, executes via /relay). This report covers what the refresh
itself changed.

## Baseline → Result
462 tests green (+1 todo), build clean → **462 tests green (+1 todo), build
clean.** Six commits, all behavior-preserving (docs + dead code + dedup).

## What changed (applied)

**Bucket A — safe cleanups:**
- `chore` removed 5 npm scripts pointing at files deleted in the 2026-04-10
  pipeline refactor (`build-tiles`, `build-tiles:test`, `preprocess`,
  `preprocess:validate-only`, `build-tiles:full`); fixed the one in-code
  reference (`audit-tiles.ts` A8 message → `preprocess:v2`).
- `chore` removed dead `_resetPipelineLogger` export (zero callers).
- `chore(wfs-proxy)` dropped the dead CLIENT_NUMBER assignment for
  operating-territories (unconditionally overwritten by CLIENT_NUMBER1 on the
  next line; output byte-identical).
- `docs` corrected CONTRIBUTING.md wfs-proxy path (`edge/` →
  `netlify/edge-functions/`).
- `refactor` extracted triplicated `useDragDismiss` (LayerPanel, HotSpotPanel,
  CalculatorPanel) into `src/hooks/useDragDismiss.ts`; Calculator's `active`
  gate became an optional param defaulting to true.

**Mandated docs (ROADMAP `next:`):**
- Authored in-repo `ARCHITECTURE.md` (registry pattern, three-source zoom
  handoff, status model, proxy contract, invariants, pipeline). Fixed
  ROADMAP's stale references (`state/plans/...`, `tools/audit/`).

## Routed out (bucket B/D → layer-fidelity plan, not folded into the refresh)
- **WfsLayers hooks-order violation + StrictMode dev crash** (ROADMAP-mandated;
  plan Wave 1.3 — same DataLayer churn as the z-order fix, lands together).
- **Vestigial WFS fetch for tile-backed layers** (plan Wave 1.3).
- **Proxy `bbox`+`CQL_FILTER` mutual-exclusion bug** — found while verifying
  Lee's live findings: every cqlFilter layer (fish-streams, tap-deferrals,
  cutblocks supplemental) fails on every bbox fetch. Fix shape proven against
  BC's server (plan Wave 1.1).
- **Per-class raster tiles broken at source** (orphaned black/gold tiles on
  R2; no isolation themes in `build-raster-tiles.py`) — plan Wave 2.

## What was deliberately left (the "no" list)
- **visibility.ts vs filter-expressions "duplication"** — reader claim
  verified FALSE: one is a fill-color ramp (0/25/50yr reds), the other an
  opacity ramp (0/20/50yr). Different properties, different stops. The 06-02
  deferred wording stands; no trivial dedup exists.
- **page.tsx keyboard-effect dependency array** — listener re-registers on
  panel toggles; harmless and correct. Refactoring to refs adds stale-closure
  risk for zero measurable win.
- **Forest-age color constants duplicated registry/setup-layers** — guarded by
  `color-audit` at test time; merging the paths was declined 06-02 and the
  Wave 2 palette unification addresses the raster side.
- **Watershed state-machine extraction, SearchBar keyboard helper** —
  readability-only, working code, M effort. Declined.
- **`moveend` listener "stacking" claim** — non-finding; React effect cleanup
  semantics make it safe.
- **conservation-priority minZoom gating** — per ROADMAP: only with a real
  perf trace. Unchanged.
- **postcss advisory inside next's vendored deps** — npm's "fix" is
  next@9.3.3. Declined; wait for upstream.
- **Pre-existing lint noise** (28 errors in scripts/e2e) — not introduced by
  this refresh; candidate for a future hygiene pass, not bundled here.

## What to watch
- Interactive render path (DataLayer/PmtilesLayers effects) still has no unit
  tests; plan Wave 1.3 adds the first (characterization via mocks/maplibre).
  PmtilesLayers timeout/sourcedata paths and DrawTool touch remain untested.
- Two raster generators (`build-raster-tiles.py` themes vs registry palette)
  — Wave 2 unifies via generated palette; until then color drift is possible.

## Drift vs ARCHITECTURE.md
No written oracle existed at audit time (degraded mode, judged against ROADMAP
+ idiom). This refresh authored the oracle; the next pass reconciles against
it for real. Known doc-vs-code tensions recorded in the doc itself:
logging-risk scope question, story-vs-interactive split (deliberate).
