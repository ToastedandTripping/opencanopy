# Code Refresh: OpenCanopy -- 2026-08-05

## Baseline
803 tests green, tsc clean, build clean, lint 0 errors / 76 warnings, 3 high vulns (sharp/postcss via next 16.2.7)
-> 805 tests green, tsc clean, build clean, lint 0 errors / 1 warning, 0 prod vulns (branch refresh/2026-08-05b)

Two audit passes: Opus readers (surface-level, 50 findings) then Fable readers (deep,
72 findings with significantly more semantic/architectural depth). The Fable pass found
real bugs, hollow test invariants, and architectural rot the Opus pass missed entirely.

## What changed

1. **cleanup: remove 8 unused CSS vars + duplicate scale-control rule** -- 7 conservation palette vars never referenced via var(), --color-accent-hover never referenced, first .maplibregl-ctrl-scale block overridden by second (globals.css)
2. **cleanup: remove dead code** -- 2 unused WFS endpoints (results, operatingTerritories; kept plannedCutblocks for Phase D), MAP_STYLES.satellite+outdoor (no basemap switcher), LayerSource "static"/"tiles"/staticData types, fish-streams from 2 hotspot layer arrays (non-public, silently dropped), redundant __opencanopy_map onLoad, roundToSigFigs barrel re-export (registry.ts, types/layers.ts, mapConfig.ts, hotspots.ts, CanopyMap.tsx, carbon/index.ts)
3. **cleanup: gitignore data/raster-tiles-v3/** -- v1/v2 were already ignored; v3 (2,895 PNGs served from R2) was committed to git
4. **deps: remove unused turf packages, bump next 16.2.7 -> 16.3.0** -- @turf/boolean-within + @turf/difference never imported; next 16.3.0 fixes 9 high-severity CVEs (middleware bypass, DoS, SSRF); prod audit: 0 vulns (down from 3 high)
5. **lint: eslint --fix + targeted suppressions + dead vars + ref capture** -- 56 stale eslint-disable directives removed (config already disabled those rules for scripts/e2e), 3 DataLayer.tsx exhaustive-deps suppressions with rationale, StoryMap yearFilter/page.tsx timeline/page.tsx _ destructuring/HeroSection img suppressions added, LoadingContext timers.current ref capture fixed, 10 dead vars/imports/functions removed across scripts/e2e (76 warnings -> 1)
6. **fix: PmtilesLayers line paint through pickDefinedPaint** -- architectural invariant #2 gap: PMTiles line-type path used raw spread bypassing pickDefinedPaint while fill/circle/WFS-line/raster all used it correctly (DataLayer.tsx)
7. **dedup: extract prefersReducedMotion, EMPTY_FC, getFirstSymbolId** -- 4 prefersReducedMotion implementations -> 1 shared lib/a11y/reduced-motion.ts; 3 EMPTY_FC constants -> 1 shared lib/map/empty-fc.ts; 3 inline firstSymbolId lookups -> shared lib/map/layer-utils.ts (re-exported from DataLayer for existing consumers)
8. **docs: reconcile ARCHITECTURE.md with code** -- PUBLIC_LAYER_IDS gating documented, 3 proxy orphans documented, 3 hooks added (useWatershedSelection, useDeviceCapability, useDialogA11y), layout section updated (a11y/, map/, keyboard/, math/, react/, mapConfig, landing/), audit suite count 7 -> 11

## What was deliberately left (the "no" list)

- **MapLibre 6.x upgrade** -- major version, behavior-changing (WebGPU backend, rendering changes), needs react-map-gl compat check + visual regression testing. L effort, not a cleanup.
- **Style-load guard unification** -- 4 implementations (SatelliteLayers, PmtilesLayers, WfsLayers, visibility.ts) work correctly today; PmtilesLayers has a fragile split-cleanup path. Unifying risks introducing regressions in the most sensitive code (map layer lifecycle). Better as a targeted relay.
- **WFS outline honor registry style.outline** -- latent inconsistency but no current consumer affected (all outline-bearing layers are tile-backed). Documented CAVEAT in types/layers.ts is accurate.
- **Visibility toggle shared helper** -- M effort, touches DataLayer.tsx lifecycle management. Payoff < risk.
- **Hash-parsing dedup (useLayerState + useMapState)** -- would add reverse coupling between the two hooks. The duplication is 5 lines.
- **Barrel/import consistency** -- cosmetic, no behavior impact.
- **MapLike/VisibilityMap interface merge** -- test-support types in story/, low impact.
- **initPMTiles double-call** -- idempotent by design, routes never co-exist.
- **Terrain source registration dedup** -- story/map deliberately separate paths.
- **MAPTILER_KEY centralize** -- registry.ts reads the env var at module scope for satellite URL construction; importing from mapConfig would add a dependency-order constraint for low payoff.
- **Stale .claude/plans/** -- 127KB orphaned artifacts from shipped relays. Deleting commits new git objects but doesn't save repo space (still in history).
- **Dead pipeline scripts** (build-tiles-chunked.sh, build-v10-throttled.sh) -- may be useful for manual tile rebuilds on constrained machines. Not part of the documented pipeline but not harmful.
- **react 19.2.4 -> 19.2.8** -- patch level, minimal value this pass.

## Recommendations beyond this skill's scope

- **COV-1: wfs-client.fetchLayerData has zero unit tests** -- the primary data-fetching function for all WFS layers. URL construction, pagination, error handling untested. M effort, high value. Route as a standalone task.
- **The improvement scan** (`research/improvement-scan-2026-07.md`, recovered from the crashed session) identifies 8 technical improvements and 8 feature opportunities, ranked by impact/effort. Top items: break the live-QA bottleneck (CI MapTiler key), R2 cache headers, tile manifest deployment, MapLibre global-state timeline batch. These are features/infrastructure, not debt -- outside refresh scope.

## Fable pass — additional applied (commits 10-13)

9. **cleanup(fable): dead code sweep** — dead initPMTiles in StoryMap (shipped pmtiles into landing bundle for nothing since Phase 1), dead .animate-fade-in-up CSS, plannedCutblocks WFS endpoint (missed in prior sweep), phantom tiles-circle interactive id, rateLimitMap dead export, getFirstSymbolId re-export shim, 3 dead scripts/lib modules (~1,030 lines: water-subtract broken by our own turf removal, superseded property-validators, unreferenced dedup-features)
10. **fix: negative carbon clamp** — WFS can return negative PROJ_AGE_1; classifyAge catches <=0 but the density formula received raw negatives, producing negative tonnes/dollars in the public panel. One-line Math.max(0, ...) fix.
11. **cleanup: untrack 2,895 raster tiles** — prior gitignore commit didn't git rm --cached; tiles still tracked despite being served from R2.
12. **harden: fix tautological audit tests** — proxy orphan check (expect(true).toBe(true) -> assert subset-of sanctioned set), story-consistency (zero constant comparisons -> range cross-checks), opacity audit (fail-open -> fail on unevaluable expressions), zoom-handoff (stale pre-v10 comment + vacuous assertion -> actual handoff contract test). ARCHITECTURE.md hooks count corrected, stale WFS-filters-client-side claim removed.

## Fable pass — route to relay (bucket B)

- **PmtilesLayers timeout leak past unmount** — 15s source-load timeout survives cleanup; fires phantom setLayerStatus("error") on torn-down instances. Needs careful lifecycle testing.
- **COMPANY_MAP 3-way mirror** — proxy, pipeline extractor, client registry with no generator and a padding drift (extractors zero-pad CLIENT_NUMBER, proxy doesn't). Single source + consistency test.
- **CLASS_LABEL_MAP -> config-driven** — class filtering is an implicit contract keyed to legend label strings + paint JSON sniffing. Rename a legend label and class filtering silently breaks. No test pins it.
- **Dual-source status test** — re-implements DataLayer's gating logic in simulate* functions. Delete the guards from DataLayer and all 12 tests stay green. Extract status-decision to a pure function.
- **Audit test fixtures** — registry-audit and schema-audit list stale property names from pre-v10 pipeline. Derive schemas from the extractors at test time.
- **WFS error taxonomy unification** — three clients to /api/wfs, three incompatible error taxonomies. forest-carbon-client's (kind union + timeout + Retry-After) is the mature pattern.
- **Age-class taxonomy SSOT** — 6 independent encodings with opposite precedence in two classifiers (proxy: harvested wins; calculator: age wins on schema drift).
- **wfs-client.fetchLayerData** — abandoned promises in debounce, abort treated as error (flash empty + badge). Zero unit tests.
- **StatusToast stale-status-on-disable** — disabled layer's error status persists (clearLayerStatus only fires on unmount, which never happens post-P1a memo redesign).
- **applyPreset availability gate** — bypasses the validation every other write path enforces; satellite preset on keyless deploy pollutes URL.
- **pipelineHealthReport** — checks source IDs that no longer exist, reports false errors for every enabled layer.

## Fable pass — decline (bucket C)

- **Proxy CORS wildcard** — doc says "same-origin", code serves Access-Control-Allow-Origin: *. Needs Lee's intent decision (keep open or lock to site origin).
- **WfsLayers entire dormant render path** — by design, layers re-enableable.
- **Dead loadData always-true guards** — defensive code in highest-risk file; annotate, don't remove.
- **Raster-overview 0.85 hardcoded** — absorb in the deferred preset-opacity-overrides batch.
- **LayerPanel/HotSpotPanel twin, ColorSwatch/ColorDot, fmt mirror** — cosmetic refactors.
- **classFilters URL persistence** — feature decision, not cleanup.

## What to watch

- The `_context` eslint warning in wfs-proxy.ts is a Netlify handler signature requirement. Configure the rule to allow `_`-prefixed vars, or accept 1 permanent warning.
- The 3 DataLayer.tsx exhaustive-deps suppressions now have rationale comments -- if the dep arrays are changed, re-verify that the layer lifecycle doesn't teardown/recreate on prop changes.
- The shared `getFirstSymbolId` is re-exported from DataLayer.tsx for backward compat; existing consumers (tests, external imports) are unaffected.

## Drift vs ARCHITECTURE.md

**1 code gap fixed:** PmtilesLayers line paint now uses pickDefinedPaint (invariant #2).
**5 doc-stale items corrected:** PUBLIC_LAYER_IDS gating, 3 hooks, layout section, audit suite count, proxy orphan entries.
**8 invariants verified clean:** R2 URL SSOT, dual-source status rule, URL as shareable source of truth, deterministic layer z-order, story/map separation, color-audit coverage, opacity-audit coverage, no hardcoded layer-id violations.
